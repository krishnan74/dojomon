[package]
cairo-version = "=2.8.4"
name = "dojo_starter"
version = "1.0.10"

[cairo]
sierra-replace-ids = true

[scripts]
migrate = "sozo build && sozo migrate"                      # scarb run migrate
spawnPlayer = "sozo execute dojo_starter-actions spawnPlayer -c str:'Fire' --wait"    # scarb run spawn
createDojo = "sozo execute dojo_starter-actions createDojomon -c str:'Balbasaur' 60 20 20 1 str:'Grass' {0,0} --wait"
catchDojo = "sozo execute dojo_starter-actions catchDojomon -c 1 --wait"



[dependencies]
dojo = { git = "https://github.com/dojoengine/dojo", tag = "v1.0.10" }

[[target.starknet-contract]]
build-external-contracts = ["dojo::world::world_contract::world"]

[dev-dependencies]
dojo_cairo_test = { git = "https://github.com/dojoengine/dojo", tag = "v1.0.10" }
